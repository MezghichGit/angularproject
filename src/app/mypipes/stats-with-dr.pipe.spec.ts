import { StatsWithDrPipe } from './stats-with-dr.pipe';

describe('StatsWithDrPipe', () => {
  it('create an instance', () => {
    const pipe = new StatsWithDrPipe();
    expect(pipe).toBeTruthy();
  });
});
